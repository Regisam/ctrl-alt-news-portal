# Story 6.1: Redis Caching Layer

**Epic**: EPIC-7 (Sprint 6-7: Performance & Deployment)  
**Story ID**: 6.1  
**Status**: Ready  
**Sprint**: 6  
**Effort**: L (32 hours)  
**Owner**: @dev (Dex)  
**Date Created**: 2026-04-21

---

## Description

Implement Redis caching layer to improve application performance. Cache frequently accessed data (articles, users, comments, categories) with configurable TTL and cache invalidation strategies. Reduce database queries and improve API response times from ~200-500ms to <100ms for cached endpoints.

**Reference**: EPIC-7 (Performance), Story 5.4 (Analytics data caching), Roadmap Sprint 6

---

## Acceptance Criteria

- [x] Redis server running locally (Docker container or native)
- [x] Redis client configured in Express server (node-redis or ioredis)
- [x] Cache layer abstraction: `CacheService` with get/set/invalidate methods
- [x] Articles endpoint caches top articles (TTL: 5 minutes)
- [x] Categories endpoint caches all categories (TTL: 1 hour, invalidate on create/update)
- [x] User profiles cached (TTL: 10 minutes, invalidate on logout)
- [x] Comment threads cached by article (TTL: 2 minutes)
- [x] Search results cached (TTL: 5 minutes)
- [x] Cache invalidation on create/update/delete operations
- [x] Cache metrics: hit/miss ratio logged to console
- [x] Environment variables: REDIS_URL (default: redis://localhost:6379)
- [x] Connection pooling configured (max 50 connections)
- [x] Error handling: graceful fallback to database if Redis unavailable
- [x] TypeScript: 0 errors
- [x] Tests: caching behavior validated (hit/miss/invalidation)
- [ ] Performance benchmark: response times <100ms for cached endpoints

---

## Tasks

- [ ] Setup Redis Docker container (docker-compose.yml)
- [ ] Install ioredis or node-redis package
- [ ] Create CacheService abstraction (`server/src/services/cache.ts`)
- [ ] Implement get/set/invalidate/clear methods
- [ ] Configure connection pooling and error handling
- [ ] Implement cache TTL strategy (configurable per endpoint)
- [ ] Cache articles list endpoint (GET /api/articles)
- [ ] Cache categories endpoint (GET /api/categories)
- [ ] Cache user profiles (GET /api/users/:id)
- [ ] Cache comment threads (GET /api/articles/:id/comments)
- [ ] Cache search results (GET /api/search)
- [ ] Implement cache invalidation on mutations (POST/PUT/DELETE)
- [ ] Add cache metrics logging (hit/miss counts)
- [ ] Create cache invalidation strategy documentation
- [ ] Add unit tests for CacheService
- [ ] Add integration tests for cached endpoints
- [ ] Benchmark performance (before/after metrics)
- [ ] Document cache configuration in README

---

## Definition of Done

- [ ] All acceptance criteria met
- [ ] Redis caching working end-to-end
- [ ] Cache invalidation automatic on all mutations
- [ ] Performance: cached endpoints <100ms average response time
- [ ] TypeScript: 0 errors (strict mode)
- [ ] Tests: all passing (70%+ coverage for cache logic)
- [ ] Error handling: graceful fallback if Redis unavailable
- [ ] Documentation: setup instructions + cache strategy documented
- [ ] No regression: non-cached endpoints unchanged

---

## Dependencies

- **Blocked by**: None (can run in parallel with other Sprint 6 stories)
- **Blocks**: Story 6.3 (Database Optimization - depends on cache metrics), Story 7.1 (if any caching patterns depend on this)
- **Related**: Story 5.4 (Analytics uses caching patterns)

---

## Risks & Mitigations

| Risk | Impact | Mitigation |
|------|--------|-----------|
| Redis connection failure | App falls back to slow DB queries | Implement fallback to database, health checks, connection retry logic |
| Cache invalidation bug | Stale data served to users | Comprehensive testing, manual verification, conservative TTL values |
| Memory issues (Redis fills) | Redis eviction or OOM | Set maxmemory policy (allkeys-lru), monitor redis-cli INFO memory |
| Cache stampede (many requests to expired key) | Database spike after cache expiry | Implement cache-aside pattern with locks or probabilistic expiry |
| Development complexity | Harder to debug caching issues | Add cache debug mode (log all cache operations), redis-cli monitoring |

---

## Notes

- Start with simple read-through cache, upgrade to write-through if needed
- Use cache-aside (lazy loading) pattern initially
- Conservative TTL values first, optimize based on metrics
- Redis should be in same network as Express (low latency)
- Plan for cache invalidation on article updates, comment posts
- Consider cache warming for homepage data

---

## Technical Design

### CacheService API

```typescript
interface CacheService {
  get<T>(key: string): Promise<T | null>
  set<T>(key: string, value: T, ttlSeconds?: number): Promise<void>
  invalidate(pattern: string): Promise<void>
  clear(): Promise<void>
  getMetrics(): { hits: number; misses: number; hitRate: number }
}
```

### Cache Key Naming Convention

```
articles:list:page:1
articles:detail:123
categories:all
users:profile:456
comments:article:789
search:query:keyword
```

### Invalidation Rules

- `articles:*` invalidated on article create/update/delete
- `categories:*` invalidated on category create/update
- `users:profile:*` invalidated on user update
- `comments:*` invalidated on comment create/delete
- `search:*` invalidated on any article change

---

## Performance Targets

| Endpoint | Non-Cached | Cached | Target |
|----------|-----------|--------|--------|
| GET /api/articles | ~300ms | ~50ms | 80% reduction |
| GET /api/categories | ~200ms | ~20ms | 90% reduction |
| GET /api/users/:id | ~250ms | ~40ms | 80% reduction |
| GET /api/search | ~500ms | ~100ms | 80% reduction |

---

## Dev Agent Record

*To be filled by @dev during implementation*

---

## File List

**To be created:**
- `server/src/services/cache.ts` - CacheService implementation
- `server/src/__tests__/cache.test.ts` - Unit tests
- `docker-compose.yml` - Redis container config (updated)

**To be modified:**
- `server/src/routes/articles.ts` - Add caching
- `server/src/routes/categories.ts` - Add caching
- `server/src/routes/users.ts` - Add caching
- `server/src/routes/comments.ts` - Add caching
- `server/src/routes/search.ts` - Add caching
- `server/index.ts` - Initialize CacheService
- `.env.example` - Add REDIS_URL
- `package.json` - Add ioredis dependency
- `README.md` - Cache configuration docs

---

## CodeRabbit Integration

**Pre-commit Review Focus:**
- ✅ Connection security (Redis URL validation, no hardcoded passwords)
- ✅ Error handling (fallback to DB if Redis unavailable)
- ✅ Memory management (proper cleanup, TTL enforcement)
- ✅ Type safety (CacheService typing, no `any` types)
- ✅ Testing (cache hit/miss scenarios, invalidation)

**Quality Gate**: Must pass security + performance checks before merge

---

## Change Log

- **2026-04-21**: Story created by @sm (River) for Sprint 6
