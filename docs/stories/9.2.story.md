# Story 9.2: Server API Integration Tests

**Epic**: EPIC-9 (Sprint 10-11: Testing & QA)  
**Story ID**: 9.2  
**Status**: Ready for Review  
**Sprint**: 10-11  
**Effort**: M (16 hours)  
**Owner**: @dev (Dex)  
**Date Created**: 2026-04-24

---

## Description

Develop comprehensive integration tests for Express.js API endpoints. Create test suites that validate request/response handling, database integration, authentication/authorization flows, and error scenarios. Use Supertest for HTTP testing and establish patterns for testing API contracts.

**Reference**: EPIC-9 (Testing & QA), Technical Approach section  
**Prerequisite**: Story 9.1 (Unit Testing Infrastructure) ✅  
**Blocker Status**: Prerequisite for Stories 9.4 (E2E tests depend on validated API layer)

---

## Acceptance Criteria

- [x] Supertest installed and configured for Express route testing
- [x] Integration test structure created (server/__tests__/integration/)
- [x] Test patterns for common API scenarios (GET, POST, PUT, DELETE)
- [x] Authentication/authorization test coverage (JWT, session validation)
- [x] Database integration tests (mock or real database fixtures)
- [x] Error handling tests (400, 401, 403, 404, 500 responses)
- [x] API response validation tests (schema, data format)
- [x] Setup/teardown patterns for database seeding and cleanup
- [x] At least 5-10 integration test examples covering key endpoints
- [x] Coverage reporting integrated with Vitest (client tests from 9.1)
- [x] All integration tests pass locally
- [x] Documentation: integration test patterns and best practices guide
- [x] Team can run `npm run test:integration` (new script)

---

## Tasks

- [x] Install Supertest and dependencies
  - [x] npm install --save-dev supertest @types/supertest
  - [x] npm install --save-dev jest-extended (optional matchers)
- [x] Create integration test structure
  - [x] server/__tests__/integration/ directory
  - [x] Database fixture setup for tests
  - [x] Helper utilities for HTTP requests
- [x] Develop API endpoint test patterns
  - [x] GET endpoint tests (happy path + error cases)
  - [x] POST endpoint tests (validation, error handling)
  - [x] PUT/PATCH endpoint tests
  - [x] DELETE endpoint tests
- [x] Authentication/Authorization tests
  - [x] JWT token validation tests
  - [x] Protected route access control tests
  - [x] Session-based auth tests (if applicable)
  - [x] Permission/role-based access tests
- [x] Error scenario tests
  - [x] 400 Bad Request (validation errors)
  - [x] 401 Unauthorized (missing/invalid auth)
  - [x] 403 Forbidden (insufficient permissions)
  - [x] 404 Not Found
  - [x] 500 Internal Server Error
- [x] Create example test files
  - [x] server/__tests__/integration/auth.test.ts (authentication flows)
  - [x] server/__tests__/integration/articles.test.ts (CRUD operations)
  - [x] server/__tests__/integration/search.test.ts (search endpoint)
  - [x] Example: Full request-response cycle validation
- [x] Database integration setup
  - [x] In-memory database or fixture management
  - [x] Seeding strategy for test data
  - [x] Cleanup/rollback after tests
- [x] Coverage reporting
  - [x] npm run test:coverage includes integration tests
  - [x] Coverage reports generated for server/ directory
  - [x] Target: >60% overall, >80% critical paths
- [x] npm scripts configuration
  - [x] npm run test:integration (run integration tests only)
  - [x] npm run test:integration:watch (watch mode)
  - [x] npm run test (includes both unit + integration)
- [x] Documentation
  - [x] docs/guides/integration-testing.md (Supertest patterns)
  - [x] docs/guides/api-test-patterns.md (endpoint testing strategies)
  - [x] Example: Full API workflow test
- [x] Validate execution
  - [x] All tests pass locally (npm run test:integration)
  - [x] No TypeScript errors
  - [x] Coverage targets met (>60%)

---

## Definition of Done

- [x] Supertest configured and working for Express routes
- [x] At least 5-10 integration test examples passing (100% pass rate)
- [x] Coverage reporting configured and baseline established
- [x] npm scripts (test:integration, test) functional
- [x] Documentation complete with patterns (2 guides)
- [x] All tests passing locally (server integration + unit tests)
- [x] No TypeScript errors
- [x] Team can run `npm run test:integration`
- [x] Story marked: "Ready for Review"

---

## Dependencies

- **Blocked by**: Story 9.1 (Unit Testing Infrastructure) ✅
- **Blocks**: Story 9.4 (E2E tests depend on validated API)
- **Related**: Story 9.3 (Frontend tests run in parallel)
- **Requires**: Express.js server implementation in place

---

## Risks & Mitigations

| Risk | Impact | Probability | Mitigation |
|------|--------|-------------|-----------|
| **Database dependency** (tests need real/mock DB) | MEDIUM | MEDIUM | Use fixtures or in-memory DB, design for isolation |
| **Async/await complexity** (proper error handling) | MEDIUM | MEDIUM | Use async/await patterns, handle rejections properly |
| **Test data management** (fixtures diverge from schema) | MEDIUM | MEDIUM | Keep fixtures in sync with schema, automated validation |
| **Flaky tests** (timing issues, race conditions) | MEDIUM | MEDIUM | Proper cleanup, no hardcoded waits, deterministic tests |
| **Performance** (slow integration tests) | LOW | MEDIUM | Parallel execution, categorize tests (quick vs slow) |

---

## Technical Details

### Supertest Configuration Example

```typescript
import request from 'supertest';
import app from '../index';

describe('GET /api/articles', () => {
  it('should return a list of articles', async () => {
    const response = await request(app)
      .get('/api/articles')
      .expect(200)
      .expect('Content-Type', /json/);

    expect(response.body).toHaveProperty('articles');
    expect(Array.isArray(response.body.articles)).toBe(true);
  });

  it('should support pagination', async () => {
    const response = await request(app)
      .get('/api/articles?page=1&limit=10')
      .expect(200);

    expect(response.body.articles).toHaveLength(10);
    expect(response.body).toHaveProperty('total');
    expect(response.body).toHaveProperty('page', 1);
  });
});
```

### Database Fixture Pattern

```typescript
// server/__tests__/fixtures/database.ts
export async function setupTestDatabase() {
  // Initialize test DB (in-memory or fixture)
  // Seed initial data
  // Return connection/client
}

export async function teardownTestDatabase() {
  // Clear all data
  // Close connection
  // Reset state
}
```

### npm Scripts Addition

```json
{
  "test:integration": "jest --testPathPattern=integration",
  "test:integration:watch": "jest --testPathPattern=integration --watch",
  "test": "npm run test:unit && npm run test:integration"
}
```

---

## File List

**New Files**:
- ✅ `server/__tests__/integration/auth.test.ts` — Authentication endpoint tests
- ✅ `server/__tests__/integration/articles.test.ts` — Articles CRUD tests
- ✅ `server/__tests__/integration/search.test.ts` — Search endpoint tests
- ✅ `server/__tests__/fixtures/database.ts` — Database setup/teardown helpers
- ✅ `server/__tests__/fixtures/test-data.ts` — Test data builders
- ✅ `docs/guides/integration-testing.md` — Supertest patterns guide
- ✅ `docs/guides/api-test-patterns.md` — API testing strategies

**Modified Files**:
- ✅ `package.json` — Add Supertest, update npm scripts
- ✅ `server/__tests__/setup.ts` — Add integration test setup (if needed)

---

## Dev Agent Record

**Status**: Draft → Ready for implementation

**Implementation Notes**:
- Start with authentication tests (foundational for other API tests)
- Then create article/resource CRUD tests
- Finally add search and edge case tests
- Use database fixtures for deterministic tests
- Ensure cleanup between tests (no state leakage)

**Quality Gates**:
- All tests pass locally
- Coverage >60%, critical paths >80%
- No flaky tests (run 3x to validate)
- Documentation with working examples

---

## Success Criteria (Epic-Level)

By end of Story 9.2, the team will:

1. ✅ Have integration test framework (Supertest) configured
2. ✅ Understand API testing patterns via examples
3. ✅ Have baseline API coverage established
4. ✅ Be confident in API endpoint validation
5. ✅ Ready to build on tests in Story 9.3 (frontend parallel)

---

## Change Log

| Date | Agent | Action | Notes |
|------|-------|--------|-------|
| 2026-04-24 | @sm (River) | Story Created | Story 9.2 drafted from EPIC-9, ready for validation |
| 2026-04-24 | @po (Pax) | Story Validated | ✅ 10/10 checklist pass, approved for implementation |
| 2026-04-24 | @dev (Dex) | Implementation Complete | 37 integration tests created (auth, articles, search, health). All tests passing. Supertest + Vitest configured. |

---

*Last Updated: 2026-04-24 by Pax (@po)*
*Status: Ready*
