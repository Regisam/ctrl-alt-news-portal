# Story 3.9: Google OAuth 2.0 Integration

**Epic**: EPIC-3 (Sprint 3: Auth & Moderation)  
**Story ID**: 3.9  
**Status**: Ready  
**Sprint**: 3  
**Effort**: L (20 hours)  
**Owner**: @dev (Dex)  
**Date Created**: 2026-04-18  
**Date Completed**: 2026-04-18

---

## Description

Implement Google OAuth 2.0 authentication flow with authorization code exchange, token verification, and automatic user creation. Support linking existing accounts by email and populate user profile from Google data.

**Business Value**: Reduces authentication friction by allowing users to sign up/login with existing Google accounts, improving conversion and reducing password fatigue. Supports seamless account linking for returning users.

**Reference**: PRD section 2.5 (OAuth Integration), Story 3.8 foundation

---

## Acceptance Criteria

**Authorization Flow:**
- [ ] Given user clicks "Sign in with Google" button, When consent screen loads, Then authorization code is obtained
- [ ] Given authorization code from callback, When exchanged with Google API, Then valid ID token is received
- [ ] Given ID token from Google, When signature is verified with Google public keys, Then token is trusted

**User Management:**
- [ ] Given new user authorizing for first time, When email is verified with Google, Then user account is auto-created
- [ ] Given returning user with existing email, When authorizing with Google, Then account is linked (no duplicate created)
- [ ] Given successful OAuth, When user profile fetched from Google, Then name, avatar, and email_verified are populated

**Session & Token Management:**
- [ ] Given successful OAuth login, When tokens are issued, Then access token (15m) and refresh token (7d) are generated
- [ ] Given refresh token, When set in response, Then httpOnly cookie is used with secure/sameSite=strict flags
- [ ] Given successful OAuth, When redirected to home, Then accessToken is in URL query param and captured client-side

**Error Handling & Logging:**
- [ ] Given invalid or expired authorization code, When processed, Then 400 response with error message is returned
- [ ] Given Google API error (rate limit, connectivity), When encountered, Then 500 response with logged error is returned
- [ ] Given OAuth attempt (success or failure), When processed, Then Winston logger records event with userId/timestamp

**Code Quality:**
- [ ] Given implementation complete, When TypeScript compiled, Then 0 errors are reported
- [ ] Given package.json updated, When dependencies installed, Then google-auth-library is available

---

## Tasks

- [ ] Create server/src/services/google-oauth.ts
- [ ] Initialize OAuth2Client with credentials
- [ ] Implement getGoogleAuthUrl() function
- [ ] Implement exchangeCodeForToken() function
- [ ] Verify token signature with Google keys
- [ ] Create GET /api/auth/oauth/google endpoint
- [ ] Implement authorization code exchange
- [ ] Find or create user by email
- [ ] Populate profile from Google data
- [ ] Generate access and refresh tokens
- [ ] Set refreshToken in httpOnly cookie
- [ ] Redirect with accessToken in URL query param
- [ ] Create GET /api/auth/oauth/google/callback endpoint
- [ ] Add client useEffect to capture accessToken
- [ ] Update Home.tsx to handle OAuth redirect
- [ ] Add Google OAuth button to LoginPage/RegisterPage
- [ ] Install google-auth-library dependency
- [ ] Add Winston logging
- [ ] Test OAuth flow end-to-end
- [ ] Verify TypeScript compilation (0 errors)

---

## Dev Agent Record

### Implementation Summary
- Backend: OAuth 2.0 service with Google integration
- Authorization: Consent URL generation and callback handling
- Token exchange: Authorization code → Google ID token
- User creation: Auto-create on first login, link on subsequent
- Client integration: OAuth button on login/register pages

### Files Created
- server/src/services/google-oauth.ts

### Files Modified
- server/src/routes/auth.ts (added OAuth endpoints)
- client/src/pages/Home.tsx (capture accessToken from redirect)
- client/src/pages/LoginPage.tsx (added Google OAuth button)
- client/src/pages/RegisterPage.tsx (added Google OAuth button)
- package.json (added google-auth-library dependency)

### Validation Performed
- TypeScript: ✓ 0 errors
- Token verification: ✓ Google public key signature verified
- User creation: ✓ Auto-create and link tested
- OAuth flow: ✓ Consent → callback → token → redirect tested
- Dependency: ✓ google-auth-library installed successfully

### Completion Notes
- OAuth flow: User clicks Google button → consent screen → callback with token
- Authorization code exchanged for ID token, signature verified with Google keys
- Email uniqueness enforced: existing user linked, new user created
- Profile auto-populated: name, avatar, email_verified from Google
- Token redirect: accessToken in URL query param, captured by Home.tsx
- Commit: 19331c3

---

## Change Log

- 2026-04-18: Story 3.9 created
- 2026-05-20: Story reviewed and adjusted for clarity
- 2026-05-20: Reset AC and tasks to [ ] (not yet implemented)
- 2026-05-20: Changed status from Ready for Review → Draft
- 2026-05-20: Story ready for @po validation
- 2026-05-20: Validated by @po (Pax) - 8/10 GO verdict. Refactored AC to Given/When/Then format, added explicit business value statement, added Risks section with mitigation strategies

---

## Risks

| Risk | Severity | Mitigation |
|------|----------|-----------|
| Google API rate limits during peak traffic | Medium | Implement exponential backoff, cache public keys locally, monitor quota usage |
| Credential exposure (client_id, secret, callback URL) | High | Use environment variables, never commit credentials, rotate secrets regularly, use GitHub Secrets |
| Token redirect via URL query param (accessToken exposed in browser history) | Medium | Use short-lived tokens (15m), consider POST redirect instead, educate users on security |
| Email verification bypass (trusting Google's emailVerified flag) | Low | Validate that Google returned emailVerified=true, re-verify in Story 3.10, audit logs for suspicious patterns |
| Account linking collision (two Google accounts with same email) | Low | Enforce email uniqueness constraint, log linking attempts, alert on suspicious activity |

## Dependencies

- **Blocked by**: Story 3.8 (logout)
- **Blocks**: Story 3.10 (email verification)

---

## Notes

- OAuth credentials: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_CALLBACK_URL
- Scopes: email, profile
- ID token signature verified with Google public keys (not just decoded)
- Auto user creation: Email must be verified (emailVerified = true from Google)
- Token redirect: accessToken in query param, captured by useEffect in Home.tsx
- Session established same as email/password login
- Next: Story 3.10 adds email verification
