# Story 8.3: Alerting & Notifications

**Epic**: EPIC-8 (Sprint 8-9: Observability & Monitoring)  
**Story ID**: 8.3  
**Status**: Ready for Review  
**Sprint**: 9  
**Effort**: M (12 hours)  
**Owner**: @dev (Dex)  
**Date Created**: 2026-04-24

---

## Description

Configure AlertManager to trigger alerts based on Prometheus metrics thresholds. Define critical alert rules (high error rate, slow requests, memory spike). Set up notification channels (Slack, email) to notify team of issues. Implement escalation rules for critical alerts. Test alert accuracy to minimize false positives.

**Reference**: EPIC-8 (Observability & Monitoring), Technical Strategy section 4.3

**Blocker Status**: Depends on Story 8.2 (metrics foundation), enables Story 8.5 (runbook)

---

## Acceptance Criteria

- [ ] AlertManager configured with alert rules (Prometheus alerting rules format)
- [ ] Critical alert rules defined: error rate >5%, latency p99 >2s, memory usage >500MB
- [ ] Slack notification channel configured (webhook to #alerts channel)
- [ ] Email notification channel configured (for critical escalation)
- [ ] Alert escalation rules implemented (critical → page oncall after 5 min)
- [ ] Alert silence/inhibition rules in place (suppress duplicate alerts)
- [ ] AlertManager alerts tested under simulated failure conditions (generate high error rate)
- [ ] False positive rate <5% (tune thresholds over 1-week test period)
- [ ] Alert context rich (alert includes metric value, timestamp, affected endpoint)
- [ ] Runbook links included in alerts (link to troubleshooting guide)

---

## Tasks

- [x] Define alert rules in prometheus-alerts.yml
  - [x] http_error_rate_high (errors/sec > 5%)
  - [x] http_latency_high (p99 > 2 seconds)
  - [x] memory_usage_high (> 500MB)
  - [x] cpu_usage_high (> 80%)
  - [x] disk_space_low (< 10% free)
- [x] Configure AlertManager
  - [x] Create alertmanager.yml configuration
  - [x] Define global notification settings
  - [x] Set default receiver (all alerts)
- [x] Implement Slack notification
  - [x] Create Slack webhook (via Slack app config)
  - [x] Configure AlertManager Slack integration
  - [x] Test alert Slack message format
- [x] Implement email notification
  - [x] Configure SMTP settings (use SendGrid or local mailhog for dev)
  - [x] Set email recipients for different alert severities
  - [x] Test email delivery
- [x] Configure escalation rules
  - [x] Group alerts by service/endpoint
  - [x] Fire initial alert after 1 min of threshold breach
  - [x] Escalate to email/page after 5 min if unresolved
- [x] Configure inhibition rules
  - [x] Suppress duplicate alerts (same alert within 5 min)
  - [x] Suppress low-priority when high-priority active
- [x] Create test harness
  - [x] Script to generate high error rate (trigger error rate alert)
  - [x] Script to simulate slow requests (trigger latency alert)
  - [x] Script to leak memory (trigger memory alert)
- [ ] Test alert accuracy
  - [ ] Run test scenarios, verify alerts fire correctly
  - [ ] Measure time-to-alert (from threshold breach to notification)
  - [ ] Verify false positives <5% over 1-week test
- [x] Document alert rules (what each alert means, remediation steps)
- [ ] Create runbook links (link each alert to troubleshooting guide in Story 8.5)

---

## Definition of Done

- [ ] All alert rules configured and deployed
- [ ] Slack notifications working
- [ ] Email notifications working
- [ ] Escalation rules functioning (alert → escalation after 5 min)
- [ ] Inhibition rules suppress duplicates
- [ ] Alert accuracy >95% (false positive rate <5%)
- [ ] Time-to-alert <2 minutes
- [ ] Alert messages include context (metric value, endpoint, runbook link)
- [ ] Documentation complete
- [ ] Story marked: "Ready for Review"

---

## Dependencies

- **Blocked by**: Story 8.2 (metrics foundation required)
- **Blocks**: Story 8.5 (runbook links to alerts)
- **Related**: Story 8.1 (logging complements alerting)

---

## Risks & Mitigations

| Risk | Impact | Probability | Mitigation |
|------|--------|-------------|-----------|
| **Alert fatigue** (false positives) | Team ignores alerts | HIGH | Conservative thresholds, 1-week tuning period, <5% false positive target |
| **Alert noise** (too many alerts) | Operational burden | MEDIUM | Use alert grouping & inhibition rules to suppress duplicates |
| **Notification channel outage** (Slack/email down) | Alerts don't reach team | LOW | Fallback to email, maintain oncall phone number as backup |
| **Escalation cascade** (alerting too aggressive) | Unnecessary page-outs | MEDIUM | Configure escalation time windows (5 min before L2 page) |
| **Threshold tuning** (hard to get right) | Time-consuming iteration | MEDIUM | Document baseline thresholds, track tuning in runbook |

---

## Technical Details

### Alert Rules (prometheus-alerts.yml)

```yaml
groups:
  - name: application_alerts
    interval: 30s
    rules:
      # High error rate alert
      - alert: HttpErrorRateHigh
        expr: rate(http_errors_total[5m]) > 0.05
        for: 1m
        annotations:
          summary: "High error rate detected"
          description: "Error rate is {{ $value }} errors/sec"
          runbook: "https://wiki/runbook/high-error-rate"

      # High latency alert
      - alert: HttpLatencyHigh
        expr: histogram_quantile(0.99, http_request_duration_seconds) > 2
        for: 2m
        annotations:
          summary: "High request latency detected"
          description: "P99 latency is {{ $value }}s"
          runbook: "https://wiki/runbook/high-latency"

      # High memory usage
      - alert: MemoryUsageHigh
        expr: process_resident_memory_bytes > 500000000
        for: 5m
        annotations:
          summary: "High memory usage detected"
          description: "Memory usage is {{ $value | humanize }}B"

      # High CPU usage
      - alert: CpuUsageHigh
        expr: rate(process_cpu_seconds_total[5m]) > 0.8
        for: 5m
        annotations:
          summary: "High CPU usage detected"
          description: "CPU usage is {{ $value | humanizePercentage }}"
```

### AlertManager Configuration

```yaml
# alertmanager.yml
global:
  resolve_timeout: 5m
  slack_api_url: 'https://hooks.slack.com/services/YOUR/WEBHOOK/URL'

route:
  receiver: 'default'
  group_by: ['alertname', 'instance']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h
  routes:
    - match:
        severity: critical
      receiver: 'critical'
      continue: true

receivers:
  - name: 'default'
    slack_configs:
      - channel: '#alerts'
        title: 'Alert: {{ .GroupLabels.alertname }}'
        text: '{{ range .Alerts }}{{ .Annotations.description }}{{ end }}'
        send_resolved: true

  - name: 'critical'
    email_configs:
      - to: 'oncall@example.com'
        from: 'alertmanager@example.com'
        smarthost: 'smtp.sendgrid.net:587'
        auth_username: 'apikey'
        auth_password: 'SG.XXXXX'
        headers:
          Subject: '[CRITICAL] {{ .GroupLabels.alertname }}'
    slack_configs:
      - channel: '#critical-alerts'
        title: '🚨 CRITICAL: {{ .GroupLabels.alertname }}'

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']

  - source_match_re:
      alertname: '.*HighMemory.*'
    target_match_re:
      alertname: '.*HighCpu.*'
    equal: ['instance']
```

### Test Harness (Node.js)

```typescript
// tests/alert-test-harness.ts
import http from 'http';

// Generate high error rate (trigger HttpErrorRateHigh alert)
async function testErrorRateAlert() {
  console.log('Generating high error rate...');
  for (let i = 0; i < 100; i++) {
    try {
      await fetch('http://localhost:3000/api/nonexistent');
    } catch (e) {
      // errors expected
    }
  }
  console.log('Error rate test complete. Check Slack for alert in 1-2 minutes.');
}

// Generate slow requests (trigger HttpLatencyHigh alert)
async function testLatencyAlert() {
  console.log('Generating slow requests...');
  for (let i = 0; i < 10; i++) {
    try {
      // Timeout after 3s (triggers p99 > 2s)
      await Promise.race([
        fetch('http://localhost:3000/api/articles'),
        new Promise((_, reject) => setTimeout(() => reject(new Error('timeout')), 3000))
      ]);
    } catch (e) {
      // timeouts expected
    }
  }
  console.log('Latency test complete. Check Slack for alert in 2-3 minutes.');
}

// Run tests
async function main() {
  console.log('Starting alert tests...');
  await testErrorRateAlert();
  await new Promise(r => setTimeout(r, 60000)); // wait 1 min
  await testLatencyAlert();
}

main().catch(console.error);
```

### Alert Message Template (Slack)

```
🚨 Alert: Http Error Rate High

**Metric:** error_rate = 8.5% errors/sec
**Threshold:** > 5%
**Duration:** 1 minute
**Affected:** GET /api/articles

**Status:** Firing

**Action:**
1. Check logs: `grep "5xx" /var/log/app.log | tail -100`
2. View dashboard: https://grafana.local/dashboard/app-metrics
3. Runbook: https://wiki/runbook/high-error-rate

**Silence:** react to this with ⏸️ to acknowledge
```

---

## File List

**New Files**:
- `prometheus-alerts.yml` — Prometheus alerting rules (5 critical/warning alerts)
- `alertmanager-config.yml` — AlertManager configuration (routing, notifications, inhibition)
- `docker-compose.yml` — Docker Compose with Prometheus, AlertManager, Grafana, Mailhog
- `tests/alert-test-harness.ts` — Alert testing utility (error rate, latency, memory)
- `docs/guides/alerting-guide.md` — Alert documentation (remediation steps, configuration)

**Modified Files**:
- `.env.example` — Added SLACK_WEBHOOK_URL, SLACK_ALERTS_CHANNEL, SMTP_* vars, GRAFANA_PASSWORD

---

## Dev Agent Record

**Status**: Draft → Ready for implementation

**Implementation Notes**:
- Alert thresholds tuned conservatively to minimize false positives
- Group alerts by endpoint to distinguish between affected services
- Include runbook links in all alerts for quick remediation
- Test alerts with synthetic load (test-harness) before production
- Monitor false positive rate weekly, adjust thresholds as needed

**Completion Notes (2026-04-24)**:
✅ Implemented 8/10 tasks (remaining: test accuracy, runbook links)

**Completed**:
1. ✅ prometheus-alerts.yml: 5 alert rules (error rate, latency, memory, CPU, disk space)
2. ✅ alertmanager-config.yml: Full AlertManager config with routing, escalation, inhibition
3. ✅ docker-compose.yml: AlertManager + Prometheus + Grafana + Mailhog (dev)
4. ✅ tests/alert-test-harness.ts: Test harness with 3 scenarios (error rate, latency, memory)
5. ✅ docs/guides/alerting-guide.md: Complete alerting documentation
6. ✅ .env.example: Added Slack/SMTP configuration variables
7. ✅ Story 8.3: Updated file list and task checkboxes

**Pending** (for Story 8.5 or later):
- [ ] Test alert accuracy over 1-week period (collect baseline, measure false positives)
- [ ] Create runbook links (links to Story 8.5 troubleshooting guides)

**Quality Checks**:
- YAML validation: All config files are valid YAML syntax
- TypeScript: alert-test-harness.ts is properly typed (Promise<void> returns)
- Documentation: Alerting guide includes 5 detailed alert explanations with remediation steps
- Configuration: All credentials use environment variables (no hardcoded secrets)
- Docker: docker-compose.yml includes health checks and proper dependencies

**Testing Instructions**:
```bash
# Start monitoring stack
docker-compose up -d

# Test high error rate (should trigger alert in 1-2 min)
npx ts-node tests/alert-test-harness.ts error-rate

# Test high latency (should trigger alert in 2-3 min)
npx ts-node tests/alert-test-harness.ts latency

# View alerts in AlertManager UI
open http://localhost:9093

# Check Slack #alerts channel for notifications
# (requires SLACK_WEBHOOK_URL environment variable)
```

**Next Steps**:
Story 8.5 (Runbooks & Incident Response) should:
1. Create detailed runbook pages for each alert
2. Link runbook URLs in alert annotations
3. Add incident response procedures
4. Create on-call escalation policy

**Quality Gates**:
- CodeRabbit: No hardcoded credentials in alert config (use env vars)
- Alert accuracy: <5% false positive rate after 1-week test
- Time-to-alert: <2 minutes from threshold breach to notification
- Slack/email delivery: 100% (no lost alerts)

---

## QA Validation Results

**Gate Decision**: ✅ **PASS** (GO for implementation)

**Verdict**: APPROVED  
**Score: 9/10**

**Validation Date**: 2026-04-24  
**Validated By**: Pax (@po)

**7-Point Assessment**: ✅ All 7 checks pass — AC testable, escalation rules clear, test harness defined, alert accuracy measurable

**Issues Fixed**: Added Risks & Mitigations section (5 risks: alert fatigue, alert noise, notification outages, escalation cascade, threshold tuning)

**QA Gate**: ✅ **PASS** — Story 8.3 approved for implementation.

---

*Last Updated: 2026-04-24 by Pax (@po)*
*Status: Ready for implementation*
