# Story 2.2: Integrar CommentsSection com Backend `/api/comments`

**Status:** InProgress
**Sprint:** 2
**Epic:** Sprint 2 Backend Integration
**Assigned to:** @dev (Dex)
**Created:** 2026-04-18
**Complexity:** 13 points (Medium-High)

---

## Story

**As a** site reader  
**I want** to post comments on articles and see them persisted  
**So that** I can engage with other readers and contribute to the discussion

---

## Description

The CommentsSection component (`client/src/pages/ArticleDetail.tsx` lines 437-443) currently uses mock comment data. This story implements real backend persistence by:

1. Creating GET/POST endpoints for comments (`/api/comments` and `/api/comments/:id`)
2. Implementing comment threading (replies to comments)
3. Validating comment content server-side
4. Storing comments in database using existing `Comment` Prisma model (Sprint 1)
5. Connecting frontend CommentsSection to real API instead of mock data
6. Adding pagination for large comment threads

---

## Acceptance Criteria

### AC1: List Comments Endpoint
**Given** a GET request to `/api/comments?articleId=<id>&page=1&limit=20`  
**When** article exists and has comments  
**Then** returns 200 + array of comments with: id, content, author, createdAt, parentCommentId (for threading)

### AC2: Create Comment Endpoint
**Given** POST to `/api/comments` with: `{ articleId, content, authorId, parentCommentId? }`  
**When** data is valid  
**Then** returns 201 + created comment object with id, createdAt, etc.

### AC3: Comment Validation
**Given** POST `/api/comments` with invalid/empty content  
**When** content is < 5 chars or > 5000 chars  
**Then** returns 400 + error: "Comment must be 5-5000 characters"

### AC4: Comment Threading
**Given** comment is reply to another comment (parentCommentId provided)  
**When** POST `/api/comments` succeeds  
**Then** comment stored with parentCommentId, frontend displays as nested reply

### AC5: Frontend Integration
**Given** user is viewing ArticleDetail page  
**When** page loads  
**Then** CommentsSection fetches comments from GET `/api/comments?articleId=X` (not mock)

### AC6: Add Comment Form
**Given** user fills comment form and clicks "Post Comment"  
**When** form is valid and POST succeeds  
**Then** new comment appears in thread immediately + form clears

### AC7: Error Handling
**Given** API request fails (500, network, timeout)  
**When** user submits comment or page loads  
**Then** error message displays: "Failed to load/post comments. Please try again."

---

## Scope

### IN
- Create GET `/api/comments?articleId=<id>&page=<n>&limit=<n>` endpoint
- Create POST `/api/comments` endpoint with validation
- Use existing `Comment` Prisma model (created in Sprint 1)
- Implement comment threading support (parentCommentId)
- Add pagination (page, limit params)
- Connect CommentsSection component to real API
- Remove mock data from frontend
- Add loading/error states to comments section
- Request logging via Winston

### OUT
- Comment moderation UI (defer to Sprint 3)
- Comment deletion (user can delete own comments) — defer to Sprint 3
- Comment editing — defer to Sprint 3
- Nested comment depth limiting (only 2 levels) — defer to Sprint 3
- Spam detection — defer to Sprint 3

---

## Dependencies

- ✅ Sprint 1 complete (`Comment` model exists in Prisma schema)
- ✅ Sprint 1 error handling middleware
- ✅ Sprint 1 Winston logger
- Story 2.1 should be completed first (similar pattern)

---

## Complexity Breakdown

| Dimension | Score | Notes |
|-----------|-------|-------|
| Scope | 3/5 | 2 endpoints, threading logic, pagination |
| Integration | 3/5 | CommentsSection has complex nested structure |
| Infrastructure | 1/5 | Existing Comment model, no new tables |
| Knowledge | 2/5 | Pagination + threading slightly complex |
| Risk | 3/5 | Comments are user-generated, moderation needed later |
| **Total** | **13** | **Medium-High** |

---

## Business Value

**Impact:** High - Comments drive engagement and user retention  
**Priority:** High - Part of professional blog features  
**ROI:** Increased time-on-site, repeat visits, community building

---

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|-----------|
| XSS via comment content | Medium | High | Sanitize input server-side, React escapes |
| Spam comments | High | Medium | Add moderation in Sprint 3 |
| Deep nesting (comments on comments on comments) | Low | Medium | Limit parentCommentId depth to 2 levels |
| N+1 queries (loading author for each comment) | Medium | Medium | Use Prisma `include: { author: true }` |
| Large thread performance (1000+ comments) | Low | High | Pagination (default 20/page) |

---

## Criteria of Done

- [ ] GET `/api/comments` endpoint with pagination
- [ ] POST `/api/comments` endpoint with validation
- [ ] CommentsSection component fetches from API (not mock)
- [ ] Comment form posts to API
- [ ] Threading works: replies show under parent comment
- [ ] Loading states visible during fetch
- [ ] Error states visible on API failures
- [ ] All comments requests logged via Winston
- [ ] Pagination working (page, limit params)
- [ ] TypeScript strict mode: 0 errors
- [ ] ESLint: 0 warnings on client/src
- [ ] Manual testing: post comment → see in database → refresh page → comment persists
- [ ] Code review passed (CodeRabbit + @qa)

---

## File List

**Frontend (Modified):**
- `client/src/components/CommentsSection.tsx` — Connect to API, remove mock

**Backend (New/Modified):**
- `server/src/routes/comments.ts` — New GET/POST endpoints (CREATED)
- `server/src/routes/index.ts` — Registered comments route (MODIFIED)
- `prisma/schema.prisma` — Verify `Comment` model exists (from Sprint 1)

**Tests:**
- `client/src/__tests__/comments.test.ts` — API integration + threading tests

---

## Dev Notes

### Data Model (Existing from Sprint 1)

```prisma
model Comment {
  id                String   @id @default(cuid())
  content           String   @db.Text
  articleId         String
  authorId          String
  parentCommentId   String?  // For threading (reply to another comment)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  deletedAt         DateTime? // Soft delete

  article           Article  @relation(fields: [articleId], references: [id], onDelete: Cascade)
  author            User     @relation(fields: [authorId], references: [id], onDelete: Cascade)
  parentComment     Comment? @relation("Replies", fields: [parentCommentId], references: [id], onDelete: Cascade)
  replies           Comment[] @relation("Replies")

  @@index([articleId])
  @@index([authorId])
  @@index([parentCommentId])
  @@index([createdAt])
}
```

### Endpoint: GET `/api/comments`

**Query Params:**
- `articleId` (required): UUID of article
- `page` (optional, default: 1): pagination page
- `limit` (optional, default: 20): comments per page
- `sort` (optional, default: "newest"): "newest" or "oldest"

**Response (200 OK):**
```json
{
  "success": true,
  "data": [
    {
      "id": "cly9x8zq20000qz8z9x9x9x9x",
      "content": "Great article! Totally agree with your take on AI ethics.",
      "author": {
        "id": "user-123",
        "name": "Jane Smith",
        "avatar": "initials or gravatar"
      },
      "createdAt": "2026-04-18T10:30:00Z",
      "parentCommentId": null,
      "replies": [
        {
          "id": "cly9x8zq20000qz8z9x9x9x9y",
          "content": "Thanks! I've been following this debate closely.",
          "author": { "id": "user-456", "name": "Marcus Chen" },
          "createdAt": "2026-04-18T11:15:00Z",
          "parentCommentId": "cly9x8zq20000qz8z9x9x9x9x"
        }
      ]
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 47,
    "totalPages": 3
  }
}
```

### Endpoint: POST `/api/comments`

**Request Body:**
```json
{
  "articleId": "article-123",
  "content": "Excellent piece. The section on neural networks was particularly insightful.",
  "authorId": "user-789",
  "parentCommentId": null
}
```

**Response (201 Created):**
```json
{
  "success": true,
  "data": {
    "id": "cly9x8zq20000qz8z9x9x9xzz",
    "content": "Excellent piece...",
    "author": { "id": "user-789", "name": "Alice Johnson" },
    "createdAt": "2026-04-18T12:00:00Z",
    "parentCommentId": null
  }
}
```

### Threading Logic

**Example thread structure:**
```
Comment A (parentCommentId: null)
├── Reply A1 (parentCommentId: A)
└── Reply A2 (parentCommentId: A)

Comment B (parentCommentId: null)
└── Reply B1 (parentCommentId: B)
    └── Reply B1a (parentCommentId: B1) ← LIMIT TO 2 LEVELS MAX
```

**Frontend render:** Display top-level comments, with replies indented below.

---

## Testing

### Unit Tests (Backend)
- [ ] GET /api/comments?articleId=<id> returns comments for that article
- [ ] GET /api/comments?page=2&limit=10 returns correct page
- [ ] POST /api/comments with valid data → 201
- [ ] POST /api/comments with empty content → 400
- [ ] POST /api/comments with content < 5 chars → 400
- [ ] POST /api/comments with parentCommentId → stores correctly
- [ ] Replies query returns threaded structure
- [ ] Soft deletes: deletedAt comments excluded from results

### Integration Tests (Frontend)
- [ ] CommentsSection loads comments on mount
- [ ] Posting comment triggers POST API call
- [ ] New comment appears in list immediately
- [ ] Replies display under parent comment
- [ ] Loading spinner visible while fetching
- [ ] Error message displays on API failure

### Manual Testing
- [ ] Load ArticleDetail page → comments load from API
- [ ] Post comment → appears immediately in section
- [ ] Reply to comment → shows as nested under parent
- [ ] Refresh page → comments still there (database persisted)
- [ ] Check database directly → comments inserted correctly

---

## CodeRabbit Integration

**Pre-Review Scan:** Yes, auto-run on commit
**Severity Filter:** CRITICAL, HIGH only
**Focus Areas:**
- XSS via comment content → Server-side sanitization + React escaping
- SQL injection → Prisma parameterized queries
- N+1 queries → Use `include` correctly in Prisma
- Pagination bypass → Validate page/limit are positive integers
- Missing error handling → All API paths must have try-catch

**Expected Issues:** Possible N+1 warnings (use `include: { author: true, replies: true }`)

---

## Change Log

| Date | Author | Change |
|------|--------|--------|
| 2026-04-18 | Quinn (@qa) | Status: Draft → Ready (validation GO, 9/10) |
| 2026-04-18 | Quinn (@qa) | Story created from QA gate Issue #2 |
