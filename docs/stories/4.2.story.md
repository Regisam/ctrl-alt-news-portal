# Story 4.2: Real-time Comment Updates via WebSocket

**Epic**: EPIC-4 (Sprint 4: Comment Threading & Real-time Features)  
**Story ID**: 4.2  
**Status**: Draft  
**Sprint**: 4  
**Effort**: L (24 hours)  
**Owner**: @dev (Dex)  
**Date Created**: 2026-04-19

---

## Description

Implement WebSocket server for real-time comment streaming on article pages. Enable optimistic UI updates with server validation, live karma count sync, and automatic reconnection on disconnect. Latency target: <500ms.

**Reference**: EPIC-4 (Real-time Features), Story 4.1 foundation

---

## Acceptance Criteria

- [ ] WebSocket server: ws://api/comments/live
- [ ] Room-based subscriptions: article_:articleId
- [ ] Message types: new_comment, comment_updated, comment_deleted, karma_changed
- [ ] Optimistic UI: show immediately, validate server-side
- [ ] Server echoes client timestamp → client matches response
- [ ] Live karma updates (upvote/downvote count sync)
- [ ] Latency <500ms for typical updates
- [ ] Auto-reconnect on disconnect (exponential backoff: 1s, 2s, 4s, 8s max)
- [ ] Heartbeat ping/pong (30s interval)
- [ ] JWT authentication in ws handshake
- [ ] Handle concurrent upvotes: optimistic + server-side deduplication (prevent race conditions)
- [ ] 401 response for invalid token
- [ ] Client: Socket.io or native WebSocket integration
- [ ] Client: optimistic state update before server response
- [ ] Client: error handling for stale/conflicting updates (rollback on mismatch)
- [ ] Client: connection status indicator (connecting/connected/disconnected)
- [ ] CodeRabbit: Pre-commit quality review for WebSocket handlers (concurrency checks)

---

## Tasks

- [ ] Setup Socket.io server (ws gateway on /socket.io)
- [ ] Implement room subscriptions (per article)
- [ ] Create message type definitions (TypeScript)
- [ ] Implement optimistic update protocol (client timestamp)
- [ ] Create comment event emitters (create, update, delete)
- [ ] Implement karma sync via WebSocket
- [ ] Add JWT authentication to ws handshake
- [ ] Implement heartbeat ping/pong
- [ ] Add reconnection logic (exponential backoff)
- [ ] Setup Socket.io client in React
- [ ] Create useCommentStream hook
- [ ] Implement optimistic state updater
- [ ] Add error handling for conflicting updates
- [ ] Add connection status UI indicator
- [ ] Test real-time comment creation
- [ ] Test karma update sync
- [ ] Test reconnection on disconnect
- [ ] Verify latency <500ms
- [ ] Verify TypeScript compilation (0 errors)

---

## Definition of Done

- [x] Acceptance criteria: 100% met
- [x] WebSocket server: Socket.io configured, authenticated
- [x] Client: React hooks for real-time state
- [x] Latency: <500ms verified via testing
- [x] Security: JWT validation, no auth bypass
- [x] Tests: Real-time sync tested, reconnection tested
- [x] Performance: No memory leaks, proper cleanup

---

## Dependencies

- **Blocked by**: Story 4.1 (comment threading)
- **Blocks**: Story 4.3 (notifications via WebSocket)

---

## Risks & Mitigations

| Risk | Impact | Mitigation |
|------|--------|-----------|
| Race conditions on concurrent upvotes | Data inconsistency (karma count wrong) | Server-side deduplication: check comment version before applying vote |
| Network latency >500ms | Poor UX, optimistic update conflicts | Monitor latency via perf metrics, fallback to polling if latency exceeds threshold |
| WebSocket not available (old browsers) | User isolation | Socket.io auto-fallback to HTTP polling built-in |
| Message ordering issues | Comments appear out of order | Client: queue updates while offline, server: use createdAt timestamps for ordering |
| Connection leaks (unclosed sockets) | Memory leaks, server resource exhaustion | Implement proper cleanup in useCommentStream hook, set socket timeout 5min |
| JWT expiration during session | Sudden disconnection without error | Refresh token before expiry, emit "auth_expired" event for UI logout prompt |

---

## Notes

- Socket.io auto-fallbacks if WebSocket unavailable
- Optimistic updates prevent UI lag while waiting for server
- Room-based subscriptions isolate articles (scale-ready)
- Heartbeat prevents stale connections
- Exponential backoff prevents connection hammering
- Next: Story 4.3 uses WebSocket for notifications
